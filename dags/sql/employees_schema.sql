CREATE TABLE IF NOT EXISTS employees (
            Serial_Number NUMERIC PRIMARY KEY,
            Company_Name TEXT,
            Employee_Markme TEXT,
            Description TEXT,
            Leave INTEGER
);
